<h1>This is the campground page</h1>

<% campgrounds.forEach(function(campgrounds){ %>
    <div>
        <h4><%= campgrounds.name %></h4>
        <img src="<%=campgrounds.image%>" alt="image of camps">
    </div>
<% }); %>